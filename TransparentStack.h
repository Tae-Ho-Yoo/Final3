#ifndef TRANSPARENTSTACK.H
#define TRANSPARENTSTACK.H

using namespace std;

template <typename T>
class TransparentStack::stack {
	
	public:
		T peek_at(size_t idx){
			T node;
			for (unsigned int = c.size() - 1; i > idx; i--){
				node = c.top;
			}
			return node;
		}
};

#endif

